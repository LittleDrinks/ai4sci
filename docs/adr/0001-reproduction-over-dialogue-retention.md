# 以复现代替原始对话留存
Agent 的原始对话轨迹在沉淀到图谱后默认丢弃，避免噪声污染后续研究。计算实验保留代码、输入、配置和随机种子，质疑通过审核代码或由独立 Agent 确定性重跑节点处理；重跑差异与原结果并存并标出依赖路径，交由独立 Agent 预审或人工审核，确认严重违反事实后使依赖结论全部失效。重跑成本高且结果可能变异的湿实验保留完整证据。该选择放弃逐字回看，以可复现性和领域重跑成本决定证据保留强度。
