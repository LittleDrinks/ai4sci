# 系统拥有节点身份
Agent 只提交行动内容，不提交可被图谱信任的节点 ID。系统对规范化内容生成不可变 UID：同一内容重复提交映射到同一身份，相同自报 ID 的不同内容保持独立。UID 只表达内容身份，不推断语义重合；两个行动是否共享机制仍由带来源的局部审核关系表达。SearchBench 的 Luna 对照中，blind 与 reflect 曾对不同内容返回相同 `candidate_id`，证明 Agent ID 不能承担图谱主键。
