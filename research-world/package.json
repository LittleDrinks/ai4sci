{
  "name": "research-world-app",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm --prefix web run build",
    "test": "uv run pytest -q && npm --prefix web run build",
    "start": "uv run research-world serve --port 8095"
  }
}
